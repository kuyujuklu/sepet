// One horizontal gutter for the whole app. Every screen-level list, header and
// card row uses it, so the left edge of a title lines up with the left edge of
// the cards under it.
export const SCREEN_PADDING = 16;

// Vertical distance between cards in a list
export const CARD_GAP = 12;
